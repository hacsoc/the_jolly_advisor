/ the user should already be signed in if we've gotten to this point
- if current_user.wishlist.include? course
  = link_to "Remove from my wishlist", wishlist_path(course_id: course.id, url: request.original_url), method: :delete
-else
  = link_to "Add to my wishlist", wishlist_path(course_id: course.id, url: request.original_url), method: :post
